# ── zsh-autosuggestions custom options ──────────────────────────────
# Subtle grey, italic suggestion
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=#666666,italic'

# Stop suggesting on very long lines
ZSH_AUTOSUGGEST_BUFFER_MAX_SIZE=80

# Extra key-bindings
bindkey '→' autosuggest-accept        # right-arrow accepts
bindkey  End autosuggest-accept